/**
 * Browser stub for Node's worker_threads. wavesurfer.js (or a dependency) may
 * reference it; this prevents Vite from externalizing it and warning in the console.
 */
export const Worker = undefined;
export default { Worker };
